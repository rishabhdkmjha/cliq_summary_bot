services:
  - type: web
    name: cliq-summary-bot
    runtime: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    healthCheckPath: /
```

Push this to GitHub, then Render will automatically use these settings!

---

## ✅ **After Fixing:**

The build should succeed with these logs:
```
==> Installing dependencies...
==> Successfully installed flask gunicorn torch transformers...
==> Build successful 🎉
==> Deploying...
==> Running 'gunicorn app:app --bind 0.0.0.0:$PORT'
Starting Flask App...
==> Your service is live 🎉
